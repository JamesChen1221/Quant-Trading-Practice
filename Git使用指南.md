# Git 版本控制使用指南

## ✅ 已完成設定

Git 倉庫已成功初始化！

```
✓ Git 倉庫初始化完成
✓ .gitignore 已設定
✓ 第一次提交完成 (v1.0.0)
✓ 所有檔案已納入版本控制
```

---

## 📚 基本 Git 指令

### 查看狀態

```bash
# 查看目前檔案狀態
git status

# 查看提交歷史
git log

# 查看簡短的提交歷史
git log --oneline

# 查看詳細的提交歷史（含變更內容）
git log -p
```

### 日常工作流程

#### 1. 修改檔案後查看變更

```bash
# 查看哪些檔案被修改
git status

# 查看具體修改內容
git diff

# 查看特定檔案的修改
git diff calculate_indicators.py
```

#### 2. 將變更加入暫存區

```bash
# 加入所有變更
git add .

# 加入特定檔案
git add calculate_indicators.py

# 加入多個檔案
git add calculate_indicators.py README.md
```

#### 3. 提交變更

```bash
# 提交並附上訊息
git commit -m "修改說明"

# 提交並附上詳細訊息
git commit -m "簡短標題

詳細說明內容
- 修改項目 1
- 修改項目 2"
```

---

## 🔄 常用操作範例

### 範例 1：修改程式碼後提交

```bash
# 1. 修改 calculate_indicators.py
# 2. 查看變更
git status
git diff calculate_indicators.py

# 3. 加入暫存區
git add calculate_indicators.py

# 4. 提交
git commit -m "修正: 改善 RSI 計算精度"
```

### 範例 2：新增功能

```bash
# 1. 新增或修改多個檔案
# 2. 查看所有變更
git status

# 3. 加入所有變更
git add .

# 4. 提交
git commit -m "新增: 支援多種技術指標計算

- 新增 MACD 計算功能
- 新增 Bollinger Bands 計算
- 更新文件說明"
```

### 範例 3：更新文件

```bash
# 修改文件後
git add README.md 使用說明.md
git commit -m "文件: 更新使用說明和範例"
```

---

## 🏷️ 版本標籤

### 建立標籤

```bash
# 建立輕量標籤
git tag v1.0.0

# 建立附註標籤（推薦）
git tag -a v1.0.1 -m "版本 1.0.1: 修正格式保留問題"

# 查看所有標籤
git tag

# 查看特定標籤資訊
git show v1.0.0
```

### 標籤命名建議

```
v1.0.0 - 主要版本
v1.1.0 - 次要版本（新功能）
v1.0.1 - 修補版本（bug 修正）
```

---

## 🌿 分支管理

### 基本分支操作

```bash
# 查看所有分支
git branch

# 建立新分支
git branch feature-new-indicator

# 切換分支
git checkout feature-new-indicator

# 建立並切換到新分支（快捷方式）
git checkout -b feature-new-indicator

# 合併分支
git checkout master
git merge feature-new-indicator

# 刪除分支
git branch -d feature-new-indicator
```

### 分支命名建議

```
master          - 主分支（穩定版本）
develop         - 開發分支
feature-xxx     - 新功能分支
bugfix-xxx      - 錯誤修正分支
hotfix-xxx      - 緊急修正分支
```

---

## 📝 提交訊息規範

### 建議格式

```
類型: 簡短描述（不超過 50 字）

詳細說明（可選）
- 變更項目 1
- 變更項目 2
```

### 常用類型

- **新增** (feat): 新功能
- **修正** (fix): 錯誤修正
- **文件** (docs): 文件更新
- **樣式** (style): 格式調整（不影響程式碼）
- **重構** (refactor): 程式碼重構
- **效能** (perf): 效能改善
- **測試** (test): 測試相關
- **建置** (build): 建置系統或依賴更新

### 範例

```bash
git commit -m "新增: 支援 MACD 指標計算"
git commit -m "修正: Excel 格式保留問題"
git commit -m "文件: 更新 README 使用說明"
git commit -m "重構: 優化資料處理邏輯"
```

---

## 🔙 復原操作

### 復原未提交的變更

```bash
# 復原特定檔案的修改
git checkout -- calculate_indicators.py

# 復原所有未提交的修改
git checkout -- .

# 取消暫存（但保留修改）
git reset HEAD calculate_indicators.py
```

### 復原已提交的變更

```bash
# 查看提交歷史
git log --oneline

# 復原到特定提交（保留變更）
git reset --soft HEAD~1

# 復原到特定提交（丟棄變更）
git reset --hard HEAD~1

# 復原到特定提交 ID
git reset --hard f994c68
```

⚠️ **注意**：`--hard` 會永久刪除變更，請謹慎使用！

---

## 🔍 查看歷史

### 查看提交歷史

```bash
# 基本歷史
git log

# 簡短歷史
git log --oneline

# 圖形化歷史
git log --graph --oneline --all

# 查看最近 5 筆提交
git log -5

# 查看特定檔案的歷史
git log calculate_indicators.py

# 查看特定作者的提交
git log --author="JamesChen"
```

### 查看變更內容

```bash
# 查看特定提交的變更
git show f994c68

# 查看兩個提交之間的差異
git diff f994c68 HEAD

# 查看特定檔案在兩個提交之間的差異
git diff f994c68 HEAD calculate_indicators.py
```

---

## 🌐 遠端倉庫（GitHub/GitLab）

### 連接遠端倉庫

```bash
# 新增遠端倉庫
git remote add origin https://github.com/username/repo.git

# 查看遠端倉庫
git remote -v

# 推送到遠端
git push -u origin master

# 推送標籤
git push --tags
```

### 從遠端更新

```bash
# 拉取最新變更
git pull origin master

# 只下載不合併
git fetch origin
```

---

## 💡 實用技巧

### 1. 查看特定時間的程式碼

```bash
# 查看昨天的程式碼
git log --since="1 day ago"

# 查看上週的程式碼
git log --since="1 week ago"

# 查看特定日期的程式碼
git log --since="2026-02-01" --until="2026-02-05"
```

### 2. 搜尋提交訊息

```bash
# 搜尋包含特定關鍵字的提交
git log --grep="RSI"

# 搜尋修改特定內容的提交
git log -S "calculate_rsi"
```

### 3. 暫存工作進度

```bash
# 暫存目前的修改
git stash

# 查看暫存列表
git stash list

# 恢復暫存的修改
git stash pop

# 恢復特定的暫存
git stash apply stash@{0}
```

### 4. 比較分支

```bash
# 比較兩個分支
git diff master..develop

# 查看分支的提交差異
git log master..develop
```

---

## 📋 .gitignore 說明

目前的 `.gitignore` 設定會忽略：

```
✓ Python 快取檔案 (__pycache__, *.pyc)
✓ 虛擬環境 (venv/, env/)
✓ IDE 設定檔 (.vscode/, .idea/)
✓ 臨時檔案 (*.log, *.tmp, *.bak)
✓ Excel 臨時檔案 (~$*.xlsx)
✓ 測試檔案 (交易資料範例.xlsx)
```

### 修改 .gitignore

如果不想追蹤 Excel 資料檔案：

```bash
# 編輯 .gitignore，加入：
量化交易.xlsx

# 如果已經追蹤，需要移除追蹤
git rm --cached 量化交易.xlsx
git commit -m "停止追蹤 Excel 資料檔案"
```

---

## 🎯 建議工作流程

### 日常開發

```bash
1. 開始工作前
   git status          # 確認狀態
   git pull            # 更新程式碼（如果有遠端倉庫）

2. 開發過程中
   # 定期查看變更
   git status
   git diff

3. 完成功能後
   git add .
   git commit -m "新增: 功能描述"

4. 重要版本
   git tag -a v1.1.0 -m "版本說明"

5. 推送到遠端（如果有）
   git push origin master
   git push --tags
```

### 實驗新功能

```bash
1. 建立新分支
   git checkout -b feature-experiment

2. 在新分支開發
   # 修改程式碼
   git add .
   git commit -m "實驗: 新功能"

3. 測試成功後合併
   git checkout master
   git merge feature-experiment

4. 刪除實驗分支
   git branch -d feature-experiment
```

---

## 🆘 常見問題

### Q: 如何復原最後一次提交？

```bash
# 保留變更，只取消提交
git reset --soft HEAD~1

# 完全復原（包含變更）
git reset --hard HEAD~1
```

### Q: 如何修改最後一次提交訊息？

```bash
git commit --amend -m "新的提交訊息"
```

### Q: 如何查看某個檔案的歷史版本？

```bash
# 查看歷史
git log calculate_indicators.py

# 查看特定版本的內容
git show f994c68:calculate_indicators.py
```

### Q: 不小心刪除了檔案怎麼辦？

```bash
# 如果還沒提交
git checkout -- 檔案名稱

# 如果已經提交
git log -- 檔案名稱
git checkout 提交ID -- 檔案名稱
```

---

## 📚 進階學習資源

- [Git 官方文件](https://git-scm.com/doc)
- [Pro Git 書籍](https://git-scm.com/book/zh-tw/v2)
- [GitHub 學習實驗室](https://lab.github.com/)
- [Git 視覺化學習](https://learngitbranching.js.org/)

---

## 🎉 總結

你現在已經有一個完整的 Git 版本控制系統！

**目前狀態：**
- ✅ Git 倉庫已初始化
- ✅ 第一次提交完成 (v1.0.0)
- ✅ 所有檔案已納入版本控制
- ✅ .gitignore 已設定

**下一步：**
1. 每次修改後記得提交
2. 重要版本建立標籤
3. 考慮推送到 GitHub/GitLab 進行備份

**快速參考：**
```bash
git status              # 查看狀態
git add .               # 加入所有變更
git commit -m "訊息"    # 提交
git log --oneline       # 查看歷史
git tag v1.0.1          # 建立標籤
```

祝你版本控制順利！🚀
