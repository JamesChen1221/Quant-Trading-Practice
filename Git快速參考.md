# Git 快速參考卡

## 🚀 最常用指令

```bash
# 查看狀態
git status

# 查看變更
git diff

# 加入所有變更
git add .

# 提交
git commit -m "訊息"

# 查看歷史
git log --oneline
```

---

## 📝 日常工作流程

```bash
# 1. 修改檔案
# 2. 查看變更
git status
git diff

# 3. 加入暫存區
git add .

# 4. 提交
git commit -m "類型: 簡短描述"

# 5. 查看結果
git log --oneline
```

---

## 🏷️ 版本標籤

```bash
# 建立標籤
git tag -a v1.0.1 -m "版本說明"

# 查看標籤
git tag

# 查看標籤詳情
git show v1.0.1
```

---

## 🔙 復原操作

```bash
# 復原未提交的修改
git checkout -- 檔案名稱

# 取消暫存
git reset HEAD 檔案名稱

# 復原最後一次提交（保留變更）
git reset --soft HEAD~1

# 復原最後一次提交（丟棄變更）⚠️
git reset --hard HEAD~1
```

---

## 🌿 分支操作

```bash
# 查看分支
git branch

# 建立並切換分支
git checkout -b 分支名稱

# 切換分支
git checkout 分支名稱

# 合併分支
git merge 分支名稱

# 刪除分支
git branch -d 分支名稱
```

---

## 🔍 查看資訊

```bash
# 簡短歷史
git log --oneline

# 圖形化歷史
git log --graph --oneline --all

# 查看特定提交
git show 提交ID

# 查看檔案歷史
git log 檔案名稱

# 搜尋提交訊息
git log --grep="關鍵字"
```

---

## 💾 暫存工作

```bash
# 暫存目前修改
git stash

# 查看暫存列表
git stash list

# 恢復暫存
git stash pop
```

---

## 🌐 遠端操作

```bash
# 新增遠端倉庫
git remote add origin URL

# 推送到遠端
git push -u origin master

# 推送標籤
git push --tags

# 從遠端更新
git pull origin master
```

---

## 📋 提交訊息類型

- **新增** (feat): 新功能
- **修正** (fix): 錯誤修正
- **文件** (docs): 文件更新
- **重構** (refactor): 程式碼重構
- **效能** (perf): 效能改善
- **樣式** (style): 格式調整

---

## ⚠️ 注意事項

1. 提交前先 `git status` 確認狀態
2. 使用 `--hard` 前要三思（會永久刪除變更）
3. 定期提交，保持提交訊息清晰
4. 重要版本建立標籤
5. 推送前先 `git pull` 更新

---

## 🆘 緊急救援

```bash
# 我不小心刪除了檔案！
git checkout -- 檔案名稱

# 我想取消最後一次提交！
git reset --soft HEAD~1

# 我想看之前的版本！
git log --oneline
git checkout 提交ID

# 我想回到最新版本！
git checkout master
```

---

**詳細說明請參考：[Git使用指南.md](Git使用指南.md)**
